# Agent instructions

This repo is one Chrome MV3 extension (WXT). The Git root is the package.

Implement, test, and build here (`npm run dev` / `test` / `build`). Domain language: [`CONTEXT.md`](./CONTEXT.md). Architecture: [`docs/architecture.md`](./docs/architecture.md).
